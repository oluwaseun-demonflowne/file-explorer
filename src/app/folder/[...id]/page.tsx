import FolderPage from "@/components/FolderPage";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <FolderPage />
    </div>
  );
};

export default page;
